# Variáveis usadas por docker-compose.prod.yml no servidor da API.
# Copie para .env.prod, preencha e rode:
#   docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build

WEB_ORIGIN=https://seu-dominio-na-vercel.vercel.app
JWT_SECRET=gere-uma-chave-aleatoria-longa-e-unica
JWT_EXPIRES_IN=7d

DB_NAME=atlas_das_cinzas
DB_USER=atlas
DB_PASSWORD=troque-esta-senha

SRD_API_BASE_URL=https://www.dnd5eapi.co
SRD_SYNC_ON_START=true
